# Solicitar a base e altura do triângulo
base = float(input("Digite a base do triângulo: "))
altura = float(input("Digite a altura do triângulo: "))

# Calcular a área
area = (base * altura) / 2

# Exibir o resultado
print(f"A área do triângulo é: {area} unidades quadradas.")
